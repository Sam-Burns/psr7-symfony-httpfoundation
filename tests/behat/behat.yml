default:

    suites:

        blackbox:
            paths:    [ %paths.base%/features/feature_files ]
            filters:  { tags: ~wip }
            contexts: [ BlackBoxContext ]

        whitebox:
            paths:    [ %paths.base%/features/feature_files ]
            filters:  { tags: ~wip }
            contexts: [ WhiteBoxContext ]
